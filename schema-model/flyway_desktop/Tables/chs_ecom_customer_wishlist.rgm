table "flyway_desktop"."chs_ecom_customer_wishlist" {
  columns = (
    column "id" {
      type = bigint unsigned
      null = NOT NULL
    }
    column "customer_id" {
      type = varchar(50)
    }
    column "name" {
      type = varchar(100)
    }
    column "sharable" {
      type = tinyint
      null = NOT NULL
      default = 1
    }
    column "is_default" {
      type = tinyint
      null = NOT NULL
      default = 0
    }
    column "default_image_url" {
      type = varchar(500)
    }
    column "created_by" {
      type = varchar(50)
    }
    column "created_on" {
      type = datetime
      default = CURRENT_TIMESTAMP
    }
    column "modified_by" {
      type = varchar(50)
    }
    column "modified_on" {
      type = datetime
      default = CURRENT_TIMESTAMP
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

index "flyway_desktop"."chs_ecom_customer_wishlist"."id_UNIQUE" {
  columns = <
    {
      name = id
    }

  >
  type = BTREE
  unique = UNIQUE
}

