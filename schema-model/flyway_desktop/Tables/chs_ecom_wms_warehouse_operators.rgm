table "flyway_desktop"."chs_ecom_wms_warehouse_operators" {
  columns = (
    column "id" {
      type = bigint
      null = NOT NULL
    }
    column "warehouse_id" {
      type = bigint
      null = NOT NULL
    }
    column "emp_code" {
      type = varchar(50)
      null = NOT NULL
    }
    column "user_id" {
      type = varchar(50)
      null = NOT NULL
    }
    column "status" {
      type = enum('ACTIVE','DISABLED')
      default = 'ACTIVE'
    }
    column "created_on" {
      type = datetime
    }
    column "created_by" {
      type = varchar(50)
    }
    column "modified_on" {
      type = datetime
    }
    column "modified_by" {
      type = varchar(50)
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

index "flyway_desktop"."chs_ecom_wms_warehouse_operators"."id" {
  columns = <
    {
      name = id
    }

  >
  type = BTREE
  unique = UNIQUE
}

