table "flyway_desktop"."chs_ecom_pms_section" {
  columns = (
    column "id" {
      type = bigint unsigned
      null = NOT NULL
    }
    column "slug" {
      type = varchar(500)
    }
    column "title" {
      type = varchar(100)
    }
    column "description" {
      type = varchar(1000)
    }
    column "file_id" {
      type = varchar(50)
    }
    column "image_url" {
      type = varchar(500)
    }
    column "status" {
      type = enum('ACTIVE','DISABLED')
      default = 'ACTIVE'
    }
    column "created_on" {
      type = datetime
      default = CURRENT_TIMESTAMP
    }
    column "created_by" {
      type = varchar(50)
    }
    column "modified_on" {
      type = datetime
      default = CURRENT_TIMESTAMP
    }
    column "modified_by" {
      type = varchar(50)
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

index "flyway_desktop"."chs_ecom_pms_section"."ft_section_title_slug" {
  columns = <
    {
      name = title
    }

    {
      name = slug
    }

  >
  type = FULLTEXT
}

index "flyway_desktop"."chs_ecom_pms_section"."id_UNIQUE" {
  columns = <
    {
      name = id
    }

  >
  type = BTREE
  unique = UNIQUE
}

