table "flyway_desktop"."dev" {
  columns = (
    column "id" {
      type = int
      null = NOT NULL
    }
    column "title" {
      type = varchar(45)
    }
    column "code" {
      type = varchar(45)
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

index "flyway_desktop"."dev"."id_UNIQUE" {
  columns = <
    {
      name = id
    }

  >
  type = BTREE
  unique = UNIQUE
}

