table "flyway_desktop"."chatbot_response" {
  columns = (
    column "id" {
      type = varchar(50)
      null = NOT NULL
    }
    column "type" {
      type = enum('WELCOME','OPTION_PROMPT','INTERMEDIATE','END')
      null = NOT NULL
    }
    column "text" {
      type = text
      null = NOT NULL
    }
    column "created_on" {
      type = datetime
    }
    column "modified_on" {
      type = datetime
    }
    column "created_by" {
      type = varchar(50)
    }
    column "modified_by" {
      type = varchar(50)
    }
  )
  primaryKey = {
    columns = [
      id
    ]
    name = "PRIMARY"
    objectType = primaryKey
  }
}

